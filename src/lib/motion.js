// src/lib/motion.js
// Keep numerically identical to --motion-ease-premium / --motion-duration-*
// in src/app/globals.css — CSS transitions and Motion animate configs must
// stay in lockstep.

export const EASE_PREMIUM = [0.16, 1, 0.3, 1];
export const DURATION = { micro: 0.12, standard: 0.2, panel: 0.32 };
