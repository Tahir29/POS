import StoreSelectionGrid from '@/components/features/auth/StoreSelectionGrid';

/**
 * Store Selection page — composition root only.
 * All logic and markup live in StoreSelectionGrid.
 */
export default function StoreSelectionPage() {
  return <StoreSelectionGrid />;
}